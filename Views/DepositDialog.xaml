<Window x:Class="HomeAccounting.Views.DepositDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock x:Name="TbLblName" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBox x:Name="TbName" Margin="0,0,0,12" Padding="6,4"/>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="130"/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="TbLblAccount" FontWeight="Bold" Margin="0,0,0,4"/>
                <ComboBox x:Name="CbAccount" DisplayMemberPath="Name" Padding="6,4"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock x:Name="TbLblCurrency" FontWeight="Bold" Margin="0,0,0,4"/>
                <ComboBox x:Name="CbCurrency" DisplayMemberPath="Code" Padding="6,4"/>
            </StackPanel>
        </Grid>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="TbLblAmount" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBox x:Name="TbAmount" Padding="6,4"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock x:Name="TbLblRate" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBox x:Name="TbRate" Padding="6,4"/>
            </StackPanel>
        </Grid>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="TbLblOpen" FontWeight="Bold" Margin="0,0,0,4"/>
                <DatePicker x:Name="DpOpen"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock x:Name="TbLblClose" FontWeight="Bold" Margin="0,0,0,4"/>
                <DatePicker x:Name="DpClose"/>
            </StackPanel>
        </Grid>

        <TextBlock x:Name="TbLblNote" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBox x:Name="TbNote" Height="46" TextWrapping="Wrap" AcceptsReturn="True" Padding="6,4"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button x:Name="BtnOk" Width="110" Height="30" Margin="0,0,16,0" IsDefault="True" Click="BtnOk_Click"/>
            <Button x:Name="BtnCancel" Width="110" Height="30" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
